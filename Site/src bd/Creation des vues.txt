CREATE VIEW InfoClient AS
    SELECT noclient,nomclient,adresse,ville,telephone,courriel
    FROM CLIENT;

CREATE VIEW InfoProduit AS
    SELECT noproduit,description,prix,exballage,quantiteenstock,fournisseur
    from produit;